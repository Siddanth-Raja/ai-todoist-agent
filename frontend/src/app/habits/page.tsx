import { PlaceholderPage } from "@/components/placeholder-page";

export default function HabitsPage() {
  return (
    <PlaceholderPage
      title="Habits are waiting behind the main planning flow."
      description="The next pass can track recurring routines, recovery signals, and streak context."
      accent="moss"
    />
  );
}
