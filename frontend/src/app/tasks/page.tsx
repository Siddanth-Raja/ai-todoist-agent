import { PlaceholderPage } from "@/components/placeholder-page";

export default function TasksPage() {
  return (
    <PlaceholderPage
      title="Tasks will connect after the chat loop is stable."
      description="This page will become the Todoist-backed focus list with triage, defer, and next-action states."
      accent="gold"
    />
  );
}
