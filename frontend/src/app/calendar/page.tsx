import { PlaceholderPage } from "@/components/placeholder-page";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar view is parked for the first shell."
      description="The future version will show schedule context, protected blocks, and candidate meeting windows."
      accent="coral"
    />
  );
}
